
# to run -> docker compose up
# to stop/remove -> docker compose down

services:
  backend:
    build: ./backend
    environment:
      - PORT=5000
    ports:
      - 8000:3000

  
  frontend:
    build: ./fronend
    env_file:
      - ./frontend/.env
    ports:
      - 5173:5173

#example

  redis:
    image: redis
    ports:
      - 6379:6379
    
