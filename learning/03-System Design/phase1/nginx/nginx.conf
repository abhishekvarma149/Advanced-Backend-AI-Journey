events{

}

http{

    upstream backend{
        server server1:5000;
        server server2:5000;
    }
    server{
        listen 80;
        location / {
            proxy_pass http://backend;
        }
    }
}